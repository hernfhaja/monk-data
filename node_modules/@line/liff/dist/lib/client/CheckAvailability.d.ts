import { LiffCore } from '../liff';
export declare class CheckAvailability {
    private static get SDK_VERSION_SUPPORTING_NEW();
    private impl;
    constructor(liff: LiffCore);
    invoke(apiName: string): boolean;
}
